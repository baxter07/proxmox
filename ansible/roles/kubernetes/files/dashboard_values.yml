
app:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - <domain>
      - kubernetes.local.lan

metrics-server:
  enabled: true

cert-manager:
  enabled: false

nginx:
  enabled: false
